package com.example.tsf_app;

public class LandingPage_Controller  {

    /*===============adding icons
    @FXML
    private Tab shopTab, striveTab, homeTab, streamTab, socialTab;

    private ImageView addGraphic(String imageFile)
    {
        ImageView imageView = null;
        Image image = new Image(getClass().getResourceAsStream(imageFile));
        imageView = new ImageView((Element) image); //added element
        imageView.setSize(15,15);
        return imageView;
    }

    // @Override //cause error
    public  void initialize(URL url, ResourceBundle resourceBundle)
    {
        ImageView view1 = addGraphic("msg.png");
        socialTab.setGraphic(socialTab.getGraphic());
        ImageView view2 = addGraphic("stream.png");
        streamTab.setGraphic(streamTab.getGraphic());
        ImageView view3 = addGraphic("briefcase.png");
        striveTab.setGraphic(striveTab.getGraphic());
        ImageView view4 = addGraphic("pngwing.com.png");
        homeTab.setGraphic(homeTab.getGraphic());
        ImageView view5 = addGraphic("shopping-trolley-icon.png");
        shopTab.setGraphic(shopTab.getGraphic());
    }

    */
}
